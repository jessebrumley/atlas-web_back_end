#!/usr/bin/env node

/**
 * displayMessage - prints a string to STDOUT
 * @param {string} message - the message to print
 */
function displayMessage(message) {
  console.log(message);
}

module.exports = displayMessage;
