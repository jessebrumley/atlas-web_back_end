#!/usr/bin/env python3
"""
This function takes a list that can contain both integers and floats,
calculates their sum, and returns the result as a float.
"""
from typing import List, Union


def func(input: type) -> :
    """
    Sums a list of integers and floats.

    Args:
        mxd_lst (List[Union[int, float]]): A list of integers and/or floats.

    Returns:
        float: The sum of the numbers in the list, represented as a float.
    """
def sum_mixed_list(mxd_lst: List[Union[int, float]]) -> float:
    return float(sum(mxd_lst))
